package com.new_app.new_app

import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity() {
    // flutter_inappwebview should auto-register with Flutter embedding v2
    // If issues persist, ensure you run: flutter clean && flutter pub get
}
